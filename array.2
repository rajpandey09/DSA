#include<iostream>
#include<vector>
#include<limits.h>
using namespace std;

int findUnique(vector<int>aarr){
    int ans = 0;
    for(int i=0;i<aarr.size();i++){
        ans = ans^aarr[i];
    }
    return ans;
}
int main(){
   /* vector<int> arr;//declare
    cout<<arr.size()<<endl;//no. of elements present
    cout<<arr.capacity()<<endl;//total no. of elementsitcanhold
    //insert
    arr.push_back(5);
    arr.push_back(6);
    //print
    for(int i=0;i<arr.size();i++){
        cout<<arr[i]<<" ";
    }
    cout<<endl;
    //remove
    arr.pop_back();
    //print
    for(int i=0;i<arr.size();i++){
        cout<<arr[i]<<" ";
    }
    cout<<endl;
    vector<int> brr(10,1);//10 elements of 1, agr only 10 then it will store all element as 0 by default
    cout<<brr.size()<<endl;
    cout<<brr.capacity()<<endl;
    for(int i=0;i<brr.size();i++){
        cout<<brr[i]<<" ";
    }
    //also we can intilaize vector by using vector<int> crr{10,20,30,40,50}; 
    
//find unique element every element occur twice except one
    int n;
    //takeing array size by user
    cout<<"enter the size of element";
    cin>>n;
    vector<int>aarr(n);
    //input
    cout<<"enter the elements:";
    for(int i=0;i<aarr.size();i++){
        cin>>aarr[i];
    }
    int uniqueElement = findUnique(aarr);
    cout<<"Unique element is:"<<uniqueElement<<endl;

    //union of 2 number when they have no duplicate
    int arr1[] = {1,3,5,7};
    int arr2[] = {2,4,6,8};
    int size1 = 4;
    int size2 = 4;
    vector<int>array;
    for(int i =0;i<size1;i++){
        array.push_back(arr1[i]);}
    
        for(int i =0;i<size2;i++){
            array.push_back(arr2[i]);
        }

        for(int i = 0;i<array.size();i++){
            cout<<array[i];
        }

    //intersection of 2 number 
    int a1[6]= {1,2,6,3,4,5};
    int a2[4]={2,2,8,9};
    int s1=6,s2=4;
    vector<int>arr;
    for(int i=0;i<s1;i++){
        for(int j=0;j<s2;j++){
            if(a1[i]==a2[j]){
                a2[j]=-1; //mark because if we won't break it check another one's better to use intmin and intmax than -1 because agar number neg or pos hua tab
                arr.push_back(a1[i]);
            }
        }
    }
    for(int i=0;i<arr.size();i++){
        cout<<arr[i];
    }*/

    //union of sets with duplicate
    int a1[5]={1,2,3,4,5};
       int a2[4]={2,4,6,7};
       int s1 = 5;
       int s2 = 4;
       vector<int>arr;
       for(int i=0;i<s1;i++){
           arr.push_back(a1[i]);
       }
       for(int i=0;i<s2;i++){
           for(int j=0;j<s2;j++){
               if(a1[i]==a2[j]){
                  a2[j] = INT_MIN;
           }
       }
       }
       for(int i=0;i<s2;i++){
           if(a2[i]==INT_MIN){
               continue;
           }
           arr.push_back(a2[i]);
       }
       for(auto value:arr){
           cout<<value;
       }

     //pair sum
       int a[5]={10,30,40,50,70};
       int sz=5,sum=80;
       for(int i =0;i<sz;i++){
           for(int j=i+1;j<sz;j++){
               if(sum == a[i]+a[j]){
                   cout<<"("<<a[i]<<","<<a[j]<<")";
               }
           }
       }

    //triplet sum
    same as pair bus hame 3 loop chalane hai ;
    
    
    return 0;
}
